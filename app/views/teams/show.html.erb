<h1> Your Team </h1>
<br>
<%= button_to "Back", "../teams", method: "get" %>
<br>
<% @pokemon.each do |pokemon| %>
    <li>
        <%%>
        <%= link_to pokemon.name.capitalize, "/pokemons/#{pokemon.id}" %>
        <%= link_to "Delete", team_pokemon_path(TeamPokemon.find_by(pokemon_id: pokemon.id, team_id: @team.id)), method: :delete%>
    </li>

<% end %>
